# Column  

**<code>Column(props: ColumnProps): string[]</code>**  

Output column wrapper.

## Parameters  
- **`props`** <code><a href="#columnprops">ColumnProps</a></code> required

## Returns  

<code>string[]</code>

## Types

### ColumnArgs  

**Type:** <code>object</code>  

**Augments:** <code><a href="/src/global/README.md#generic">Generic</a></code>

#### Properties  
- **`tag`** <code>string</code> optional  
Default: `'div'`  
- **`width`** <code>string | number</code> optional  
Used in image utilities.  
- **`widthSmall`** <code>string | number</code> optional  
Used in image utilities.  
- **`widthMedium`** <code>string | number</code> optional  
Used in image utilities.  
- **`widthLarge`** <code>string | number</code> optional  
Used in image utilities.  
- **`classes`** <code>string</code> optional  
- **`style`** <code>string</code> optional  
- **`attr`** <code>string</code> optional

### ColumnProps  

**Type:** <code>object</code>  

**Augments:** <code><a href="/src/render/README.md#renderfunctionargs">RenderFunctionArgs</a></code>

#### Properties  
- **`args`** <code><a href="#columnargs">ColumnArgs</a></code> required