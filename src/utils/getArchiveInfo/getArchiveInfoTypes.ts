/**
 * Utils - Get Archive Info Types
 */

/**
 * @typedef {object} ArchiveInfo
 * @prop {string} id
 * @prop {string} slug
 * @prop {string} title
 */
export interface ArchiveInfo {
  id: string
  slug: string
  title: string
}
